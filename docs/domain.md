# 领域约定

医防协同随访枢纽围绕多机构慢病线索、授权和转介责任保存业务事实。主要对象为机构、患者别名、风险线索、授权、转介、责任人、访问记录、拆分审批。

必须长期遵守以下约束：迟到事件不能倒退已确认状态；授权撤回立即限制新访问；错误合并的拆分需双人批准；机构只获得完成转介所需信息。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。